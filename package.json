{
  "name": "oauth2-activitypub-example",
  "version": "1.0.0",
  "description": "ActivityPub OAuth 2.0 POC client",
  "type": "module",
  "scripts": {
    "test": "web-test-runner test/**/*.test.js --node-resolve",
    "test:watch": "web-test-runner test/**/*.test.js --node-resolve --watch"
  },
  "devDependencies": {
    "@web/test-runner": "^0.18.0",
    "@web/test-runner-commands": "^0.6.0",
    "@web/test-runner-playwright": "^0.12.0",
    "sinon": "^17.0.1"
  }
}